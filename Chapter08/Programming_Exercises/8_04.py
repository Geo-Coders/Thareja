# Write a program that converts a list of character into their corresponding ASCII values using map()function

from logging import getLogRecordFactory


list = good
result = map(ord(list))
print(result)
